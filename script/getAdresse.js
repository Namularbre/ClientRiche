$.fn.getAdresse = function (geocoder) {
    //geocoder et map son déclarer en dehors de tout module, dans la vue du menu (/views/menu.html)
    let resultat = {
        lat: null,
        lng: null,
    };
    return resultat;
}